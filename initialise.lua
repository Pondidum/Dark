local addon, ns = ...

ns.mixins = {}
ns.layouts = {}
ns.controls = {}

local core = Dark.core

ns.media = {
	textures = core.textures,
	colors = core.colors,
	fonts = core.fonts,
	sounds = core.sounds,
}

ns.layout = core.layout
